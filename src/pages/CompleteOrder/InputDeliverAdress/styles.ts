import { styled } from "styled-components";

export const InputArea = styled.div`
    display: flex;
    flex-wrap: wrap;
    align-items: flex-start;
    padding: 0px;
    gap: 1rem;
    margin-top: 2rem;

    width: 35rem;
`;